apiVersion: v1
kind: Service
metadata:
  name: poc-devops
  namespace: poc
spec:
  type: LoadBalancer
  selector:
    app: poc-devops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
